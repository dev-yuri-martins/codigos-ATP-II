#include <stdio.h>

int main() {
    int x;
    scanf(" %d\n", &x);
    
    if (x % 2 == 0) {
     int y 
    }
    
    printf(" %d\n", y);
    
    return 0;
    //Se o objetivo da questão era mostrar que dá erro de compilação então está certa
}
